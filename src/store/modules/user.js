import { UPDATE } from "../types";

const state = {
  person: 1
};

const getters = {};

const mutations = {
  [UPDATE]() {}
};

const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
};
